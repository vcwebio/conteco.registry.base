---
version: '3'
services:

  service:
    image: ${CONTECO_REGISTRY}${CONTECO_REALM}/${CONTECO_ECOSYSTEM}.${CONTECO_TYPE}.${CONTECO_NAME}:${CONTECO_TAG}
    ports:
      - 5000:5000
    deploy:
      mode: replicated
